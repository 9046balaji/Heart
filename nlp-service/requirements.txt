fastapi>=0.104.1
uvicorn>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
# spacy>=3.7.2  # Commented out due to Python 3.14 compatibility issues
nltk>=3.8.1
transformers>=4.35.2
torch>=2.1.1
scikit-learn>=1.3.2
pandas>=2.1.3
numpy>=1.26.2
requests>=2.31.0
python-multipart>=0.0.6
sentence-transformers>=5.1.0
ollama>=0.1.38
aiohttp>=3.9.1
vaderSentiment>=3.3.2
tenacity>=8.2.3
slowapi>=0.1.9  # Rate limiting for API protection (PHASE 1 TASK 1.5)

# ADK & LLM Integration (Phase 1)
google-adk>=1.2.1
google-generativeai>=0.6.0
litellm>=1.35.0

# NEW: LangChain Dependencies for LLMGateway enhancement (Phase 1)
langchain>=0.3.0
langchain-google-genai>=2.0.0
langchain-ollama>=0.2.0

# NEW: Langfuse for observability (Phase 1)
langfuse>=2.0.0

# NEW: PydanticAI for structured outputs (Phase 1)
pydantic-ai>=1.0.0

# NEW: LlamaIndex for RAG enhancement (Phase 3)
llama-index-core>=0.11.0
llama-index-llms-ollama>=0.3.0
llama-index-embeddings-ollama>=0.3.0

# NEW: Unstructured for document processing (Phase 3)
unstructured[all-docs]>=0.15.0

# NEW: CrewAI for multi-agent simulation (Phase 2)
crewai>=0.80.0

# NEW: LangGraph for orchestration (Phase 2)
langgraph>=0.2.0

# NEW: Ragas for RAG evaluation (Phase 4)
ragas>=0.2.0

# NEW: Arize Phoenix for production monitoring (Phase 4)
arize-phoenix>=5.0.0

# Health & Appointment Features (Phase 2-3)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL adapter
cryptography>=41.0.0  # For PHI encryption

# Calendar & Notifications (Phase 4 - Optional)
# Install these for full calendar/notification functionality:
# pip install google-auth google-auth-oauthlib google-api-python-client
# pip install twilio firebase-admin
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0
apscheduler>=3.10.0

# NLU Date Parsing (Optional but recommended)
dateparser>=1.2.0

# Redis Caching (Optional - for L2 cache)
# pip install redis[asyncio]
redis>=5.0.0

# ML Explainability (Optional - for risk model interpretability)
shap>=0.44.0

# ============================================================
# Medical Document Processing (medical.md implementation)
# ============================================================

# OCR Engines
pytesseract>=0.3.10  # Local OCR (requires Tesseract-OCR installed)
google-cloud-vision>=3.5.0  # Google Cloud Vision API
boto3>=1.34.0  # AWS SDK for Textract

# Medical AI / MedGemma
# google-generativeai already included above

# Notification Services
twilio>=8.10.0  # WhatsApp Business API
sendgrid>=6.11.0  # Email delivery
firebase-admin>=6.4.0  # Push notifications (FCM)

# Document Processing
python-magic>=0.4.27  # File type detection
Pillow>=10.2.0  # Image processing
PyPDF2>=3.0.0  # PDF parsing
pdf2image>=1.17.0  # PDF to image conversion

# Optional: PDF generation for reports
# weasyprint>=60.0  # HTML to PDF (has system dependencies)

# HIPAA Compliance & Security
python-jose[cryptography]>=3.3.0  # JWT tokens
passlib[bcrypt]>=1.7.4  # Password hashing
# cryptography already included above

# ============================================================

# Testing & Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
protobuf>=5.29.1


